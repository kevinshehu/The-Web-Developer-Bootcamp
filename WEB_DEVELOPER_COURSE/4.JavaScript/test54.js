// creating an object
const Kevin_Math = {
    // variable
    PI: 3.141599,

    // method
    area(radius) {
        return this.PI * radius * radius;
    },

    // method
    perimeter(radius) {
        return 2 * this.PI * radius;
    }
}